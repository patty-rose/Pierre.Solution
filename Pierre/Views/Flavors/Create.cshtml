@{
  Layout = "_Layout";
}

@model Pierre.Models.Flavor

<h1>Add a new Flavor to the Menu</h1>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)<br><br>

  <input type="submit" value="Add new flavor"/>
}